# ไม่ต้องคัดลอก node_modules เข้าไปใน container
node_modules

# log files
npm-debug.log
yarn-debug.log
yarn-error.log

# Git directory
.git

# MacOS system files
.DS_Store

# VSCode settings (ถ้ามี)
.vscode

# environment files (ถ้าไม่ต้องการให้เข้าไปใน container)
.env

# ไฟล์อื่นๆ ที่ไม่ต้องการรวมใน container
*.log
